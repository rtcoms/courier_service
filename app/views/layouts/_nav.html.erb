<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Courier</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse" aria-controls="navbarCollapse" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarCollapse">
      <ul class="navbar-nav me-auto mb-2 mb-md-0">
        <%= nav_link('Home', root_path) %>
        <%= nav_link('Courier Requests', courier_requests_path) %>
        <%= nav_link('Search', search_courier_requests_path) %>
      </ul>
      <%= render('shared/search_form')%>
      <ul class="nav">
        <% unless current_user %>
          <%= nav_link('Login', new_user_session_path) %>
          <%= nav_link('Sign up', new_user_registration_path) %>
        <% else %>
          <%= nav_link("Hi, #{current_user.email}", edit_user_registration_path) %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete  %>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
